package com.example.myLibrary

import org.gradle.api.Plugin
import org.gradle.api.Project

// TODO
@Suppress("unused")
open class MyLibraryPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        with(target) {
        }
    }
}
